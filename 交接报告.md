# 桌面 Agent 交接报告

更新日期：2026-08-15  
项目目录：`D:\桌面agent`

## 1. 项目目标与当前阶段

项目是本地运行的 A 股桌面应用，技术结构为 Electron + React + Python FastAPI。总体规划包含三块：

- 时事资讯雷达：新闻抓取、分类、筛选与来源状态。
- 股票 Agent：模型对话和受控股票数据工具。
- 股票工作台：行情、K 线、基本面及市场数据展示。

当前已完成第一块“时事资讯雷达”的基础可用版本。股票 Agent、股票工作台、模型配置和正式打包尚未开始。

## 2. 当前已完成内容

### 2.1 工程与桌面壳

- 已建立 Electron 主进程、React 渲染进程、Python FastAPI 后端。
- Electron 启动时会启动 Python 后端。
- Electron 主窗口当前加载开发地址 `http://localhost:5174/`。
- 后端默认监听 `127.0.0.1:8000`。
- 已提供受限 preload IPC：`desktopAgent.getBackendStatus()`。
- 已提供 `/health` 健康检查，React 页面会展示“后端已连接 / 后端未连接”。

### 2.2 新闻后端

已实现以下完整链路：

```text
来源配置
-> 抓取
-> 解析
-> 清洗与规范化
-> SHA-256 指纹去重
-> 一级板块 / 行业 / 主题分类
-> SQLite 存储
-> 查询与分页 API
-> 来源健康状态
```

SQLite 数据库位于 `state/news.db`，并已被 `.gitignore` 排除，不会提交到 Git。

已实现接口：

| 接口 | 用途 |
|---|---|
| `GET /health` | 后端健康检查 |
| `GET /taxonomy` | 返回 10 个一级板块、31 个行业和主题标签 |
| `GET /sources` | 返回来源健康状态 |
| `GET /news` | 新闻列表、筛选与分页 |
| `POST /refresh` | 刷新全部登记来源 |

`GET /news` 支持参数：

- `primary_sector`
- `industry`
- `topic`
- `limit`：1 至 100，默认 50
- `offset`：默认 0

返回值包含 `items`、`count`、`total`、`offset`、`limit` 和 `updatedAt`。

### 2.3 新闻分类体系

分类文件是 `config/news-taxonomy.json`。

- 一级板块：10 个。
- 二级行业：申万一级 31 个。
- 跨行业主题：AI、芯片、机器人、新能源车、低空经济、算力、创新药、消费电子、军工信息化、新型电力系统。

页面已保留“全部行业”和 10 个一级板块。左侧原先展示的 31 行业可见列表已经按需求删除；行业仍通过页面顶部下拉框筛选。

### 2.4 新闻来源状态

来源配置位于 `config/news-sources.json`，当前共登记 12 类来源。

| 状态 | 数量 | 来源 |
|---|---:|---|
| 已接入并真实抓取 | 2 | 财联社快讯、东方财富全球资讯 |
| 数据源待接入 | 10 | 东方财富个股资讯、东方财富行业研报、东方财富个股研报、巨潮资讯公告、上交所披露、深交所披露、人民银行政策、国家统计局宏观数据、新浪财经资讯、同花顺财经资讯 |

规则：

- 两个已接入来源才会执行真实抓取、解析和入库。
- 10 个待接入来源在刷新时被明确标记为 `pending`，详情是“数据源待接入”。
- 待接入来源不发起解析、不写入新闻库、不伪造新闻内容。
- 真实来源出错时状态为 `error`，并保留错误详情；不阻塞其余来源刷新。

最后一次已记录的接口验收结果：12 个来源中 2 个 `ok`、10 个 `pending`、0 个 `error`；新闻库有 179 条真实来源新闻。该数字会随用户刷新来源而变化。

### 2.5 React 资讯首页

首页当前具备：

- 后端连接状态。
- 10 个一级板块切换。
- 行业下拉筛选。
- 主题标签筛选。
- 新闻来源、发布时间、一级板块、行业和主题标签。
- 原文新窗口跳转。
- 刷新按钮。
- 来源异常提示。
- “数据源待接入”提示，不与异常混淆。
- 新闻分页：首次 50 条，“加载更多”追加后续数据。
- 切换板块、行业、主题或刷新时回到第一页。

## 3. 已完成的提交

最近与当前阶段直接相关的提交：

| 提交 | 内容 |
|---|---|
| `49d3d2d` | 12 类来源登记与待接入状态展示 |
| `e127b82` | 新闻分页加载 |
| `cd647c7` | 接入中国财经新闻来源 |
| `0c0a235` | 替换不稳定新闻源 |
| `bb85709` | 删除左侧 31 行业可见列表 |
| `5904105` | 新闻抓取与资讯看板基础版 |
| `94db59a` | 新闻分类体系 |
| `ab3f032` | Electron 后端健康状态连接 |

## 4. 关键文件说明

| 文件 | 作用 |
|---|---|
| `apps/backend/app/main.py` | FastAPI、SQLite、抓取、解析、分类、来源状态、查询 API |
| `config/news-sources.json` | 12 类来源及启用状态 |
| `config/news-taxonomy.json` | 10 板块、31 行业、主题标签 |
| `apps/renderer/src/App.tsx` | 资讯首页、筛选、刷新、分页、状态提示 |
| `apps/renderer/src/App.css` | 资讯首页样式 |
| `apps/desktop/src/main.cjs` | Electron 主进程、后端启动、窗口生命周期 |
| `apps/desktop/src/preload.cjs` | 受限 IPC 桥接 |
| `skills/a-stock-data/SKILL.md` | 已下载但尚未接入的 A 股数据 Skill |
| `桌面Agent重构计划.md` | 总体产品与阶段计划 |

## 5. 本地运行方式

### 前置条件

- Node.js 与 npm。
- Python 3.12 或与现有虚拟环境兼容的 Python。
- 后端虚拟环境位于 `apps/backend/.venv`。

### 开发启动

打开两个终端：

```powershell
cd D:\桌面agent\apps\renderer
npm run dev -- --port 5174
```

```powershell
cd D:\桌面agent\apps\desktop
npm start
```

Electron 会自动启动后端。浏览器开发页面地址是：

```text
http://localhost:5174/
```

### 构建检查

```powershell
cd D:\桌面agent\apps\renderer
npm run build
```

```powershell
cd D:\桌面agent\apps\backend
.\.venv\Scripts\python.exe -m py_compile app\main.py
```

## 6. 尚未完成内容

### 6.1 资讯模块后续工作

- 10 个待接入来源尚未实现各自稳定的抓取器和解析器。
- 目前分类主要依赖标题、摘要中是否出现行业名和主题词；行业语义识别尚未增强。
- 没有定时刷新、抓取日志表、重试退避或来源限流策略。
- 没有按来源、行业或日期的新闻统计接口。

### 6.2 模型配置

以下全部未实现：

- DeepSeek Provider。
- 本地 API Key 安全配置。
- OpenAI、Claude Provider 预留。
- 流式输出。
- 模型超时、错误和余额/配额提示。

### 6.3 Agent Runtime

以下全部未实现：

- 会话管理与持久化。
- 工具调用循环。
- 工具白名单。
- JSON 参数验证。
- 单次调用超时、调用次数限制和错误隔离。
- 可审计的工具调用过程展示。
- 统一的数据来源、更新时间和缓存状态。
- 金融安全输出层，禁止预测涨跌、买卖指令、目标价和仓位建议。

### 6.4 a-stock-data Skill

`skills/a-stock-data` 已下载，但尚未纳入应用运行时：

- 未实现 Skill 自动发现。
- 未实现元数据读取与按需加载。
- 未封装为受控股票工具。
- 模型尚不能调用 Skill。
- 尚未建立“模型不能执行任意代码或任意 URL”的运行时限制。

### 6.5 股票 Agent 页面

全部未实现：

- 多会话聊天。
- 流式中文回答。
- 行情、K 线、基本面、公告、研报工具。
- 工具调用步骤、来源和耗时展示。
- 工具失败时的结构化降级说明。

### 6.6 股票工作台数据层

全部未实现：

- 自选股。
- 市场概览。
- 单股详情。
- K 线和分时。
- 基本面。
- 公告与研报。
- 行业概念。
- 资金流。
- 涨停池。

### 6.7 桌面应用完善

当前 Electron 生命周期只有基础版本，以下未实现：

- 动态端口分配与端口冲突处理。
- 开发环境和生产环境加载地址区分。
- Windows 安装包与双击启动。
- Python 打包或随应用分发。
- 退出 Electron 时后端子进程的跨平台可靠清理。

### 6.8 高级功能

以下均未实现：图表标记、策略条件、自动提醒、综合评分、决策树、多 Agent、MCP 管理。

## 7. 已知限制与注意事项

1. 当前端口是固定的：Vite 为 5174，后端为 8000。重复启动 Electron 或后台残留后端进程时，可能出现端口占用和旧代码仍在响应的问题。
2. 当前 Electron 主窗口硬编码了开发地址，因此不能视为生产版桌面应用。
3. 新闻数据库是本地 SQLite 文件；不要把 `state/news.db` 加入 Git。
4. `skills/` 目录目前是未追踪目录，不应在未确认许可范围前整体提交。
5. 10 个待接入来源是明确登记状态，不是已经可用的抓取源；不要为了显示内容而填充示例新闻。
6. 新闻源协议和页面结构会变化。新增某个来源前，应先单独验证响应、频率限制、解析结果和原文链接。

## 8. 建议接手顺序

建议按以下顺序推进，避免先做界面后缺少真实数据：

1. 为一个“待接入来源”实现真实抓取、解析和独立验收，再逐个扩展其余来源。
2. 建立 Provider 配置与 API Key 本地安全存储。
3. 建立受控 Agent Runtime：会话、工具契约、白名单、参数校验、超时、审计与金融安全层。
4. 把 `a-stock-data` 的少量高价值能力先封装为受控工具，例如实时行情、K 线、基本面。
5. 实现股票 Agent 的最小聊天闭环。
6. 在真实工具稳定后实现股票工作台数据层。
7. 最后处理动态端口、生产构建和 Windows 打包。

## 9. 交接验收清单

- [x] React 构建已通过。
- [x] Python `app/main.py` 语法检查已通过。
- [x] `/news` 分页验证：前两页各 50 条，页面间无重复。
- [x] 12 来源状态验证：2 个可用，10 个待接入。
- [x] 待接入来源不产生新闻内容。
- [x] 当前阶段提交已完成。
- [ ] Electron 生产构建与 Windows 安装包。
- [ ] 模型与股票数据能力。
